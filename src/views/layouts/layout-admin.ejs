<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | Mural da Fé Admin
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cardo:ital,wght@0,400;0,700;1,400&family=Cinzel:wght@400;700&display=swap"
        rel="stylesheet">
    <!-- Tailwind CSS (CDN for simplicity in MVP) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        gold: '#D4AF37',
                        wine: '#5B0e2D',
                        primary: '#0A0A0A',
                    },
                    fontFamily: {
                        serif: ['Cardo', 'serif'],
                        display: ['Cinzel', 'serif'],
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Cardo', serif;
        }

        h1,
        h2,
        h3,
        h4,
        h5,
        h6 {
            font-family: 'Cinzel', serif;
        }
    </style>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col">

    <% if (!locals.hideNav) { %>
        <nav class="bg-primary text-white shadow-md border-b border-gold/30">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex items-center justify-between h-16">
                    <div class="flex items-center">
                        <a href="/admin/dashboard" class="flex-shrink-0">
                            <span class="font-display text-gold font-bold text-xl tracking-wider">MURAL DA FÉ</span>
                            <span class="text-xs text-gray-400 block -mt-1 tracking-widest uppercase">Admin</span>
                        </a>
                        <div class="hidden md:block">
                            <div class="ml-10 flex items-baseline space-x-4">
                                <a href="/admin/dashboard"
                                    class="group px-3 py-2 rounded-md text-sm font-medium transition-colors <%= path === '/admin/dashboard' ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Dashboard</a>

                                <a href="/admin/intercessores"
                                    class="px-3 py-2 rounded-md text-sm font-medium transition-colors <%= path.includes('/intercessores') ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Intercessores</a>

                                <a href="/admin/comunidades"
                                    class="px-3 py-2 rounded-md text-sm font-medium transition-colors <%= path.includes('/comunidades') ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Comunidades</a>

                                <a href="/admin/gracas"
                                    class="px-3 py-2 rounded-md text-sm font-medium transition-colors <%= path.includes('/gracas') ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Graças</a>
                            </div>
                        </div>
                    </div>

                    <!-- Desktop Menu -->
                    <div class="hidden md:block">
                        <div class="ml-4 flex items-center md:ml-6">
                            <a href="/" target="_blank" class="text-gray-400 hover:text-white mr-4 text-sm">Ver Site</a>
                            <a href="/admin/logout"
                                class="bg-red-900/50 hover:bg-red-900 text-red-100 px-3 py-2 rounded-md text-sm font-medium border border-red-800 transition-colors">Sair</a>
                        </div>
                    </div>

                    <!-- Mobile Menu Button -->
                    <div class="md:hidden">
                        <button id="mobile-menu-button" type="button"
                            class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-gold">
                            <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M4 6h16M4 12h16M4 18h16" />
                            </svg>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Mobile Menu -->
            <div id="mobile-menu" class="hidden md:hidden border-t border-gray-800">
                <div class="px-2 pt-2 pb-3 space-y-1">
                    <a href="/admin/dashboard"
                        class="block px-3 py-2 rounded-md text-base font-medium <%= path === '/admin/dashboard' ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Dashboard</a>
                    <a href="/admin/intercessores"
                        class="block px-3 py-2 rounded-md text-base font-medium <%= path.includes('/intercessores') ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Intercessores</a>
                    <a href="/admin/comunidades"
                        class="block px-3 py-2 rounded-md text-base font-medium <%= path.includes('/comunidades') ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Comunidades</a>
                    <a href="/admin/gracas"
                        class="block px-3 py-2 rounded-md text-base font-medium <%= path.includes('/gracas') ? 'bg-gray-800 text-gold' : 'text-gray-300 hover:text-gold hover:bg-gray-800' %>">Graças</a>
                    <a href="/" target="_blank"
                        class="block px-3 py-2 rounded-md text-base font-medium text-gray-300 hover:text-gold hover:bg-gray-800">Ver
                        Site</a>
                    <a href="/admin/logout"
                        class="block px-3 py-2 rounded-md text-base font-medium bg-red-900/50 hover:bg-red-900 text-red-100 border border-red-800">Sair</a>
                </div>
            </div>
        </nav>

        <script>
            // Mobile menu toggle
            document.getElementById('mobile-menu-button')?.addEventListener('click', function () {
                const menu = document.getElementById('mobile-menu');
                menu.classList.toggle('hidden');
            });
        </script>
        <% } %>

            <main class="flex-grow">
                <%- body %>
            </main>

            <footer class="bg-white border-t border-gray-200 py-4 mt-8">
                <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <p class="text-center text-gray-500 text-sm">
                        &copy; 2026 Mural da Fé - Painel Administrativo
                    </p>
                    <p class="text-center text-gray-400 text-xs mt-1 font-sans tracking-wider">
                        <a href="https://corpodigital.com.br" target="_blank" rel="noopener noreferrer"
                            class="hover:text-gold transition-colors">
                            by Corpo Digital | Unidade de Performance
                        </a>
                    </p>
                </div>
            </footer>

</body>

</html>